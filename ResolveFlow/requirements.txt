# ResolveFlow 依赖清单

# ── AI / LLM ──────────────────────────────────────────────────────────────────
anthropic==0.40.0          # LLM 调用；意图 Embedding 分支支持远端客户端，缺失时本地兜底
openai==1.57.0             # LLM_PROVIDER=openai 时使用（Qwen/DashScope 等 OpenAI 兼容协议）

# ── Web 框架 ──────────────────────────────────────────────────────────────────
fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.3
python-multipart==0.0.12    # FastAPI 文件上传支持

# ── 数据存储 ──────────────────────────────────────────────────────────────────
redis==5.2.1
chromadb==0.5.23           # 向量数据库（长期记忆）

# ── 监控 ──────────────────────────────────────────────────────────────────────
prometheus-client==0.21.1

# ── HTTP 客户端 ───────────────────────────────────────────────────────────────
httpx==0.28.1              # webhook 告警与 Qwen3 Reranker HTTP 客户端

# ── 工具 ──────────────────────────────────────────────────────────────────────
python-dotenv==1.0.1
PyJWT==2.10.1               # Action 层最简 RBAC（user/reviewer/admin）签发/校验本地 JWT

# ── Postgres 迁移 POC（可选，仅 agents/action_runtime_pg_poc.py 使用）──────────
# 生产 Action 层仍是 SQLite；见 wiki/postgres-migration-plan.md。不装这个包完全
# 不影响 make test / 主应用运行。
asyncpg==0.31.0
